Use the installed swap-to-zeronui skill to migrate this entire application's UI to Zeron, including the shell, resource page and login page. Preserve route destinations, resource loading/retry and username/password submission. Replace the old visual implementation where Zeron has a matching capability. Do not introduce new product features. Implement the migration and report its actual completion state with evidence.
