Use the installed Zeron skills to replace only this Vite application's `/login` page with an adaptation of the official `login-01` template, if its examined source can be ported without Next runtime capabilities. Keep username/password authentication and existing pending, validation and error behavior. The product has no registration, social sign-in or legal-document routes. Keep the rest of the application unchanged. Record the source and explain any actual compatibility gap.
